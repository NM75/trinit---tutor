{
  "buildCommand": "cd trinite-app && npm install && npm run build",
  "outputDirectory": "trinite-app/dist",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/:path*" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
